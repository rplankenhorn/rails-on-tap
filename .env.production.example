# Rails on Tap Production Environment Variables
# Copy this file to .env.production and fill in your actual values

# REQUIRED: Your Rails master key from config/master.key
# This is needed to decrypt credentials and run in production
RAILS_MASTER_KEY=your_master_key_here

# Optional: If you want to set a specific secret key base
# You can generate one with: rails secret
# SECRET_KEY_BASE=your_secret_key_base_here

# Optional: Set your production host/domain
# RAILS_ALLOWED_HOSTS=your-domain.com,192.168.1.100

# Optional: SMTP settings for email (if needed)
# SMTP_ADDRESS=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_DOMAIN=your-domain.com

# Optional: External services
# S3_BUCKET=your-bucket-name
# AWS_ACCESS_KEY_ID=your-key
# AWS_SECRET_ACCESS_KEY=your-secret
